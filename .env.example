# Mapbox public access token (pk.*). Vite inlines VITE_* vars into the client
# bundle by design — that is expected for pk tokens, but you should restrict the
# token to your deployed origins in the Mapbox account settings:
# https://console.mapbox.com/account/access-tokens/
VITE_MAPBOX_TOKEN=pk.your_token_here
